import React from 'react';

class Header extends React.Component {

    constructor() {
        super();
    };

    render() {
        return (
            <header>
                header
            </header>
        );
    }
}

Header.defaultProps = {
};

Header.propTypes = {
};

export default Header;
